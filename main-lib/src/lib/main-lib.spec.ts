import { mainLib } from './main-lib';

describe('mainLib', () => {
  it('should work', () => {
    expect(mainLib()).toEqual('main-lib');
  });
  it('has another test', () => {
    expect(true).toBeTruthy();
  });
});
